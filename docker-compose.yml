version: "3"
services:
  api:
    container_name: oqt-api
    build: .
    environment:
      OQT_CONFIG: /config/config.yaml
      OQT_DATA_DIR: /data
      POSTGRES_HOST: oqt-database
      POSTGRES_PORT: 5432
    command: poetry run python scripts/start_api.py --host 0.0.0.0
    volumes:
      - ./data:/data
      - ./config:/config
    ports:
      - "127.0.0.1:8080:8080"
    depends_on:
      - database

  database:
    container_name: oqt-database
    build: database/
    environment:
      POSTGRES_DB: oqt
      POSTGRES_USER: oqt
      POSTGRES_PASSWORD: oqt
    ports:
      - "5445:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
